services:
  - type: web
    name: ci-analytics
    env: node
    buildCommand: npm install && npm run build
    startCommand: node ./dist/server/entry.mjs
    port: 3000
    envVars:
      - key: NODE_VERSION
        value: 22.16.0
      - key: NODE_ENV
        value: production
